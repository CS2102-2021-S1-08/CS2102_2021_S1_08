/**
 * Router for availabilities 
 */ 

const router = require("express").Router()

// TODO: use middleware checkAuthenticatedAsCareTaker
// TODO: Care Taker can view their availabilities (GET)
// TODO: Care Taker can add new availabilities (POST)
// TODO: Care Taker can remove availabilities (DELETE)

module.exports = router;